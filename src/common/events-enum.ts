export const enum StudentEvents {
    StudentDeleted = 'student-deleted'
}

export const enum CourseEvents {
    CourseDeleted = 'course-deleted'
}

export enum Grade {
    A,
    B,
    C,
    D,
    F
}